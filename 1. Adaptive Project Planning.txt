useEffect(() => {
  setGridOptions(prev => ({
    ...prev,
    // optional: also keep a context copy
    context: { ...(prev.context || {}), isNewRequest },

    // give columnDefs a NEW reference so AG Grid re-evaluates functions
    columnDefs: prev.columnDefs.map((c, i) =>
      i === 0
        ? {
            ...c,
            // if you prefer reading from closure instead of context:
            showDisabledCheckboxes: (params) => /* logic using isNewRequest */,
            checkboxSelection: (params) => /* logic using isNewRequest */,
          }
        : c
    ),
  }));
}, [isNewRequest]);
